name: Import pinned SpotiFLAC extension exports once

on:
  push:
    branches:
      - feature/openspot-vnext-extension-automix
    paths:
      - '.github/workflows/import-spotiflac-exports-once.yml'

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/openspot-vnext-extension-automix
      - name: Import exact pinned upstream export layer
        run: |
          set -euo pipefail
          curl --fail --location --silent --show-error \
            'https://raw.githubusercontent.com/todo996/SpotiFLAC-Mobile/8165fc99f18234049d003ec8379dea7792c452a1/go_backend/exports_extensions.go' \
            --output third_party/spotiflac-extension-core/upstream/exports_extensions.go
          test -s third_party/spotiflac-extension-core/upstream/exports_extensions.go
          grep -q '^func InitExtensionSystem' third_party/spotiflac-extension-core/upstream/exports_extensions.go
          grep -q '^func LoadExtensionFromPath' third_party/spotiflac-extension-core/upstream/exports_extensions.go
          grep -q '^func GetInstalledExtensions' third_party/spotiflac-extension-core/upstream/exports_extensions.go
      - name: Commit pinned export layer
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add third_party/spotiflac-extension-core/upstream/exports_extensions.go
          if git diff --cached --quiet; then
            echo 'Pinned export layer already matches.'
            exit 0
          fi
          git commit -m 'vendor(extension): import pinned SpotiFLAC public exports'
          git push origin HEAD:feature/openspot-vnext-extension-automix
